<?php self::layout('views/layout') ?>

<h1>:class #<?= $:alias->id ?></h1>

<a href="<?= url('/:alias', $:alias->id, 'edit') ?>">edit</a>
<a href="<?= url('/:alias', $:alias->id, 'delete') ?>" data-confirm="Are you sure to delete this :alias ?">delete</a>

<div class="entity">

    <?php foreach($:alias as $field => $value): ?>
    <div class="line">
        <strong><?= $field ?></strong>
        <p><?= $value ?></p>
    </div>
    <?php endforeach; ?>

</div>